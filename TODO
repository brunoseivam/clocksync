2 - ADJTIME_MASTER would be better off if it were a separate 
	processing thread. By doing this, we would avoid losing 
	messages from new nodes and the state machine would be 
	more responsive
3 - Support IPv6? =]
